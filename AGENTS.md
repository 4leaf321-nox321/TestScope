# 개발 지침

이 저장소에서 코드를 고칠 때 지키는 규칙.

**이 파일이 정본이다.** `CLAUDE.md` 는 이 파일을 가리키기만 한다 — 두 벌로 두면
한쪽만 고쳐지고, 그때부터 사람마다 다른 규칙을 따르게 된다.

## 이 시스템이 답하려는 것

    "80도 환경에서 20 kN 이상의 인장시험 가능한 장비 있어?"

    시험 항목  ->  요구 조건  ->  시험법  ->  가능한 장비  ->  보유 위치

이 한 사슬이 화면 구성과 모델 배치를 정한다. **어떤 결정이 이 사슬을 끊는다면
그것이 그 결정이 틀렸다는 신호다.**

## 구조

- **모듈 이름은 백엔드와 프론트가 같다.** `app/modules/<name>` <-> `frontend/src/modules/<name>`.
  예외는 `tests/architecture/test_boundaries.py` 에 **사유와 함께** 적는다.
- **모듈끼리 라우터를 직접 부르지 않는다.** 조립 지점은 `app/main.py` 하나다.
  모델은 서로를 참조해도 된다(FK 는 본질적으로 그렇다). 로직 공유는 `shared` 를 거친다.
- **`shared` 는 도메인 라우터를 모른다.** 방향은 shared -> 모듈 한 쪽이다.
  알면 순환이 생기고, 그때 import 순서 하나로 서버가 안 뜬다.
- 권한 판정은 `shared/permissions.py` 한 곳에서 한다. 각 쿼리에 흩뿌리면
  "목록에는 보이는데 상세는 403" 같은 어긋남이 생기고, 그때 어느 쪽이 맞는지
  알 방법이 없다.

## 데이터

- 새 ORM 모델을 만들면 **`app/all_models.py` 에 import 를 추가**한다. 빠뜨리면
  autogenerate 가 **기존 표를 지우는** 마이그레이션을 만든다. DB 를 만지는
  스크립트도 `app.all_models` 를 import 한다 — 앱에서는 안 드러나고 그 스크립트를
  돌릴 때만 터진다. `tests/architecture` 가 검사한다.
- **조건 값은 언제나 SI 로 담는다**(`ConditionKey.si_unit`). 화면이 실무 단위로
  바꿔 보여 준다. 섞어 담으면 20 이 20 N 인지 20 kN 인지 알 수 없고, 그 둘은
  자릿수가 셋 다르다. 환산은 **화면 한 곳에서만** 한다.
- **범위의 빈 칸은 "제한 없음" 이다.** 0 으로 채우지 않는다 — 하한이 0 인 장비와
  구별되지 않고, 검색이 그 차이로 갈린다.
- **카탈로그는 계열과 기종 두 층이다**(ADR 0006). 무슨 시험이 되나는
  `equipment_series` 에, 수치 사양은 `equipment_models` 에 붙는다. **보유 장비가
  가리키는 것은 기종이다** — 계열을 가리키게 두면 「300 kN 됩니까」 에 계열은
  「0.5~300 kN 입니다」 라고밖에 못 하고, 그 대답은 우리가 가진 그 한 대에 대해
  아무것도 말하지 않는다.
- **기준정보 축에는 소속이 있다**(`Vocabulary.domain`). `equipment` 보유 장비 ·
  `catalog` 카탈로그 · `method` 시험법 · `common` 여러 층이 함께 쓰는 것. 축이 일곱을
  넘으면 한 목록으로는 「이게 어디 쓰이는 값이지」 를 알 수 없고, 그때 규격 제정기관
  축에 회사 이름이 들어간다 — 그리고 그 값은 아무도 안 지운다. **`common` 은 얼버무리는
  자리가 아니다**: 시험 항목처럼 장비의 시험 항목·계열의 시험 항목·시험법이 모두 가리키는 것만 둔다.
- **화면이 기준정보의 정본이다.** 관리 화면이 축의 이름·설명·정책·속성 칸과 값의
  이름·코드·상위 값·속성·표기·병합·폐기를 전부 고친다 — 고칠 수 없는 것이 하나라도 있으면
  사람은 파일을 고쳐 다시 들이고, 그날부터 화면은 이류가 된다. 축이 값의 칸을 말한다
  (`Vocabulary.attribute_schema`): 물성은 기호·단위를 갖는데 축이 그것을 안 말하면 편집
  화면은 JSON 을 통째로 보이고 아무도 안 고친다. **축을 만드는 것만 코드다** — 축은 코드가
  걸어야 뜻이 있다. 병합은 원본을 가리키던 도메인 행을 대상으로 돌린다(`_REFERENCES`);
  안 돌리면 쓰이는 값일수록 RESTRICT 에 막힌다 — 실제로 그랬다.
- **되풀이되는 자유 문자열은 축으로 올린다.** 기종 형태와 교정 기관이 그랬다 —
  `benchtop` 이 화면에 그대로 뜨고, 같은 기관이 두 이름으로 갈릴 자리였다. 올릴 때는
  **원본 슬러그를 값의 `code` 에 남긴다**: 사람이 이름을 바꿔도 반입이 code 로 찾는다.
  시험 항목·분류·제조사도 같다 — 온톨로지 id 가 code 다. 이름으로만 찾으면 관리 화면에서
  「인장」 을 「인장 시험」 으로 바꾼 다음 반입이 「인장」 을 또 만든다. 코드는 축 안에서
  하나다(부분 유일 인덱스).
- **보유 장비는 비울 수 없는 칸이 넷이다** — 보유 부서·거점·상세위치, 그리고 무슨
  종류인가(기종을 고르면 계열에서 따라오고, 안 골랐으면 개체가 분류를 직접 가리킨다).
  전부 「없으면 그 장비를 못 찾는」 칸이다. **공용여부는 부서와 다른 칸이다**
  (`shared_use`) — 부서를 비워 공용을 나타내면 그 순간 관리 부서를 잃는다.
- **개체 사양은 복사가 아니라 겹쳐 보기다**(`equipment_spec_values`). 개체는 기종
  사양과 **다른 값만** 갖고 나머지는 기종 것이 그대로 보인다. 전부 복사하면 카탈로그가
  개정돼도 안 따라오고, 어느 값이 실측인지 구별이 사라진다. 시험 항목을 복사로 둔 것
  (ADR 0004)과 다른 판단인 이유: 시험 항목은 「그때 그렇게 판단했다」 는 스냅샷이고, 사양
  수치는 사양서와 실측이 **둘 다 남아야** 사람이 고를 수 있다.
- **옵션 부속 기준인 값은 표시로 둔다**(`requires_accessory` — 사양값·계열 조건·장비 조건).
  카탈로그는 「-180~320 °C」 를 항온조 옵션으로 적는 일이 흔하다. 비고에만 적으면 검색은
  글자를 못 읽고 「됨」 이라고 답한다 — 갖고 있지도 않은 챔버를 전제로. 표시는 사양 → 조건
  → 판정(`accessory`)까지 따라가고, 그 대에 부속이 실제로 있으면 사람이 조건에서 끈다.
  반입은 이 표시를 **켜기만** 한다 — 사람이 끈 것을 되살리지 않는다.
- **본체가 못 대는 조건은 붙는 부속이 댄다**(ADR 0008). 판정이 `unmet`·`unknown` 이어도
  계열에 관계로 이어진 챔버·노가 그 조건을 내면 `accessory` 로 답하고 **어느 기종이 얼마까지
  내는지와 보유 대수**를 같이 준다. **분류가 정한 축만 댄다** — 항온조는 온도·습도, 노는
  온도, 그립·신율계는 아무것도. 그립 사양의 내열 온도는 그립이 견디는 값이지 시험 온도가
  아니고, 그것으로 「달면 됩니다」 라고 답하면 그럴듯해서 아무도 확인하러 안 간다.
  관계는 **양방향으로 읽는다**(카탈로그에 `본체 -> 챔버` 와 `챔버 -> 본체` 가 섞여 있다).
- **사양에서 따라 들어간 시험 조건에는 꼬리표를 남긴다**(「사양 …에서 따옴」).
  그것이 없는 조건은 사람이 적은 것이라 **안 덮는다** — 구별을 포기하면 실측을 저장할
  때마다 손으로 확인한 범위가 조용히 사라진다.
- **물성은 시험 항목과 N:M 으로 잇고, 물성의 정본은 MaterialTwin 키다**(ADR 0007).
  기준정보 축 `property` 의 값은 `code` 가 `mechanical.yield_strength` 이고 한글 이름은
  바뀔 수 있다 — 코드·반입·검색은 code 로 건다. 연결(`test_item_properties`)은 기계가
  제안하고 사람이 확인한다; **제안을 확인처럼 그리지 않는다.** 확인은 **줄 단위로 묶어서**
  한다(`PATCH /test-item-properties/bulk`) — 254건을 알 하나씩 누르게 두었더니 확인이 0 인
  채로 남았다. 묶음에는 되돌리기를 같이 둔다; 없으면 사람이 아예 안 누른다. 시험 항목을 물성으로
  쪼개지 않는다 — 「인장」 은 다섯 물성을 내고, 「DSC」 는 여섯을 낸다.
- **MaterialTwin 은 또 하나의 카탈로그 출처다.** DB 를 직접 반입하지 않고
  `source/catalog/tools_from_materialtwin.py` 가 객체 JSON 으로 바꾼다 — 반입 경로가 둘이면
  같은 장비가 두 길로 들어오고 어느 쪽이 맞는지 알 수 없다. 계열 묶음은
  `source/materialtwin/groups.json`(사람이 확인한 표)이 정하고, 이미 있는 객체에는
  `supplements` 로 없는 기종만 보탠다.
- **규격과 시험 항목은 N:M 이다**(`test_method_items`). 규격은 **문서**이지 시험 항목이 아니다 —
  IEC 60529 는 IP 코드의 1자리(방진)와 2자리(방수)를 한 문서가 정의하고, MIL-STD-810 은 방법
  번호마다 다른 시험이다. 칸 하나로 두었더니 「분진 침투」 에 붙인 순간 「방수」 가 **규격이
  없는 항목**이 됐고, 그 항목으로 찾는 사람은 「그런 규격이 없다」 를 받았다(2026-09-24 실측:
  규격 0건인 시험 항목 19개 중 여럿이 이 부류). `series_test_item_methods` 와 다른 층이다 —
  그것은 「이 계열이 이 항목을 할 때 이 규격을 인용한다」 는 세 겹 관계고(ADR 0002), 이것은
  「이 규격은 이 시험의 것이다」 라는 계열과 무관한 사실이다. 검토함 `method_test_items` 는
  **여럿을 한 번에 확정**한다(`multi=True`) — 목록을 받는 틀은 처음부터 있었고 모델만 하나였다.
- **인용 규격은 비고가 아니라 표로 잇는다**(`series_test_item_methods`). 시험 항목을 규격마다
  쪼개면 검색이 같은 장비를 여덟 줄로 답하므로 「이 계열은 인장이 된다」 는 한 줄로 두고,
  규격은 옆 표에 단다. 글자로 두면 시험법 453건이 **아무것도 가리키지 않는 목록**이 된다 —
  「ASTM D638 되는 장비」 를 물으면 문자열을 훑는 수밖에 없고, 판이 바뀌어도 안 따라온다.
- **어느 시험의 규격인지 못 정한 인용은 미정 표에 남긴다**(`series_pending_methods`).
  시험이 여럿인 계열이 인용한 규격은 반입이 시험 항목을 못 정하고, 그러면 링크를 못 만든다.
  그 사실을 DB 에 안 남기면 시험법이 「가능 장비 없음」 으로 서고, **못 하는 시험과 끊긴
  연결을 아무도 못 가른다** — 464 중 287 이 그렇게 서 있었다. 시험 항목이 정해지는 순간
  (화면·반입·계열에 시험 항목 추가) `promote_pending` 이 링크로 올린다. 반입은 **이미 있는
  시험 항목에도** 빠진 링크를 보탠다 — 두 번째 반입이 첫 반입이 못 한 일을 이어받는다.
- **요구 조건은 표로 받고, 값은 지어내지 않는다**(`methods/imports.py`). 규격의 조건은 규격서
  본문에 있는 숫자라 장비 카탈로그에서 뽑을 수 없다. 규격서를 보는 사람이 엑셀에 적어
  붙여넣는다 — 장비 대장 반입과 같은 규칙(붙여넣기 · 두 걸음 · 넣을 수 있는 줄은 넣는다).
  단위가 조건의 것과 다르면 거절한다(20 N 을 kN 으로 들이면 천 배 틀린다). 틀린 조건은 빈
  조건보다 나쁘다 — 검색이 자신 있게 틀린 답을 낸다. 464 를 다 채우는 것이 목표가 아니다;
  보유 장비가 실제로 가리키는 규격(`?used=owned`)부터 채운다.
- **쓰임 수는 저장하지 않고 셀 때 센다.** 저장해 두면 갱신하는 코드를 빠뜨리고, 그때
  그 화면은 전부 0 을 보여 준다 — 실제로 그랬다. 틀린 수를 싸게 얻느니 맞는 수를 그
  값에 치른다(축 하나에 GROUP BY 두세 번).
- **시험 조건은 저장할 때가 아니라 복사할 때 합친다.** 계열 조건은 봉투이고 기종
  사양은 그 기종의 것이라, 같은 조건이 양쪽에 있으면 **기종이 이긴다**. 사양을
  저장하는 순간 계열의 시험 항목에 써 넣으면 한 기종의 값이 계열 전체의 조건이 된다.
- **정의 없는 값은 「이 기종만의 사양」 으로 들인다**(`model_free_specs`). 카탈로그 키
  950종 중 803종이 한 기종에만 나온다 — 전부 정의로 세우면 「사양 추가」 목록이 못 쓰게
  되고(ADR 0005 의 그 실패), 버리면 원문 JSON 에만 남는다. 세 자리로 가른다: 정의 있는
  사양(여러 기종이 공유, 비교·검색) · 이 기종만의 사양(한 기종, 값은 들어가고 정의는 안
  부푼다) · 원문(근거). 이름은 온톨로지가 알면 그 라벨, 모르면 **원본 키 그대로** — 기계가
  지어내면 그것이 진실이 된다. 같은 원본 키가 다른 기종에도 쌓이면 사람이 「정의로 세우기」
  로 올리고, 그때 같은 키의 다른 기종 줄도 함께 옮긴다. 수치로 못 읽는 줄(「약 300」)은
  그대로 둔다.
- **능력행의 측정 범위는 사양이다**(`CAPABILITY_SPECS`). MaterialTwin 은 기종 사양표 대신
  「무엇을 어디까지 재나」 를 물성마다 적어 온다 — 점도계는 점도 범위로 갈리고 분광기는
  파장으로 갈리므로 그것이 그 기종의 사양이다. **(물성, 단위) 쌍으로 건다**: 투과율의 범위가
  `nm` 로 적혀 있으면 그것은 투과율이 아니라 파장이다. 표에 없는 쌍은 안 들인다(원문에 남는다).
- **사양 → 검색 조건은 단위를 곱해서 옮기고, 못 곱하면 안 옮긴다**(`app/shared/units.py`,
  `conditions_from_specs`). 정의 「시험력」 은 gf, 축 「하중 용량」 은 kN — 그대로 옮기면 500 gf
  가 500 kN 이 된다. 못 맞추는 짝(쇼어 경도 ↔ kN)은 건너뛰고 사양표가 `axis_unit_mismatch`
  로 말한다. 프론트 `units.ts` 와 같은 표를 두 벌 둔다 — 서로 부를 길이 없어서.
- **단위는 사람이 곱하지 않는다**(`shared/units.ts`). 카탈로그가 「4,000 cP」 인데 정의가
  Pa·s 면, 값 칸이 단위째 받아 바꾸고 무엇을 무엇으로 바꿨는지 적는다. 모르는 단위는
  **거절하고 저장을 막는다** — 숫자만 떼어 담으면 천 배 틀린 값이 검색에 쓰인다. 온도는
  배율이 아니라 자리 옮김이라 따로 다룬다(0 °C 는 0 K 가 아니다).
- **반입은 원문을 버리지 않는다.** 정의가 있는 값은 `model_spec_values` 로, 원본은
  통째로 `raw_specs`·`raw_limits`(JSONB)로 간다. 이것은 ADR 0005 의 「JSONB 대신
  EAV」 와 어긋나지 않는다 — 거기서 물린 것은 **정의를 통제하기 위해서**였고, 이
  칸은 정의하는 자리가 아니라 보존하는 자리다(아무것도 참조하지 않고 검색도 안 본다).
  다만 **보존은 읽을 수 있어야 보존이다.** 사양이 빈 기종(891 중 224)에서는 원문이 사람이
  옮겨 적을 유일한 근거라, 화면이 그것을 펼친 채로 표로 세운다(`RawSpecs`) — 한 줄 JSON 으로
  덤프해 접어 두었더니 아무도 안 열었고, 그래서 아무도 안 채웠다.
- **분류별 대표 사양의 정본도 온톨로지다**(`categories.json` 의 `headline_specs`).
  목록 한 줄이 기종을 가르려면 어느 사양을 보여 줄지 알아야 하는데, 그 답은 분류마다
  다르다 — 만능시험기는 하중이고 챔버는 온도다. 화면이 정하면 화면마다 갈리고,
  「많이 채워진 것」 으로 자동으로 고르면 데이터가 늘 때 대표가 조용히 바뀐다.
  반입이 그것을 분류 값의 `attributes.headline_specs` 로 옮기고, 안 적은 분류는
  **검색축에 이어진 사양**으로 대신한다.
- **사양 어휘의 정본은 `source/catalog/ontology` 다.** 동의어(`aliases`)·단위
  변형(`unit_variants`)·성격(`role`)을 거기 적는다. 이쪽 손 매핑표에 적으면 같은
  지식이 두 저장소로 갈라지고, 갈라진 뒤에는 어느 쪽이 맞는지 알 방법이 없다.
- **사양은 정의를 통제하고 값은 자유롭게 둔다**(ADR 0005). 새 사양 칸은
  `spec_definitions` 에 만들고, 값은 `model_spec_values` 에 행으로 쌓는다. 정의의
  `kind` 와 `key` 는 만든 뒤에 못 바꾼다 — kind 는 값이 어느 칸에 담겼는지를 정하고,
  key 는 코드와 반입 스크립트가 걸고 있다. 안 쓸 정의는 지우지 말고 끈다.
- **사양 정의의 적용 분류가 비어 있으면 "공통" 이다.** "아직 안 정했음" 이 아니다.
  분류로 거르는 조회는 **공통도 함께** 돌려줘야 한다 — 조인으로만 거르면 전원·무게가
  어느 장비 화면에도 안 뜬다.
- 지우지 않는다. 장비·시험법·계정은 `deleted_at` 만 채운다. 그것으로 잰 데이터가
  밖에 남아 있고, 몇 년 뒤에도 그것이 무엇이었는지는 물어질 수 있다.
- 삭제·이관 기능을 만들기 전에 **무엇이 그것을 가리키는지 보여 주는 자리**를 먼저
  둔다(`workspaces` 의 `references`). 누르기 전에 아는 것이 이 저장소의 무늬다.

- **다른 시스템의 칸을 이름만 보고 잇지 않는다.** ReportArchive 의
  `external_view_default` 와 우리 `restricted` 는 글자가 비슷하지만 다른 물음에
  답한다(보고서 공개 대 장비 가시성). 게다가 기본값이 서로 반대라, 뒤집어 넣으면
  반입 직후 모든 부서의 장비가 검색에서 사라진다 — **모르는 칸은 안 옮기고 보고한다.**

## API

- 성공 응답은 리소스 그대로, 오류만 엔벨로프. 오류 코드는 `TSC-<MODULE>-<NNNN>`.
- **오류 본문을 라우트에서 직접 만들지 않는다.** `AppError` 계열을 raise 한다 —
  응답을 만드는 경로가 곧 로그를 남기는 경로여야 한다.
- **부분 수정에서 "안 보낸 것" 과 "비운 것" 을 구별한다.** 라우트가
  `model_dump(exclude_unset=True)` 로 넘긴다. 안 구별하면 상태 하나 바꿀 때마다
  담당자와 위치가 지워지고, 그 손실은 저장한 사람 눈에 안 보인다.
- 목록 엔드포인트에는 서버가 상한을 강제한다(`shared/pagination.py`).
- 스키마를 바꿨으면 `python scripts/export_openapi.py` 와 `npm run api:types` 를
  함께 돌린다. **프론트 타입을 손으로 적지 않는다** — 손으로 적은 타입은 반드시
  서버와 어긋나고, 어긋난 날 화면은 아무 말도 안 하고 undefined 를 그린다.

## 프론트

- 스타일은 Tailwind 유틸리티로 컴포넌트에 붙인다. 전역 CSS 클래스를 새로 만들지
  않는다.
- API 절대주소를 코드에 넣지 않는다. 항상 상대경로 `/api`.
- shadcn 프리미티브 위에 도메인 컴포넌트를 쌓고, 화면은 조립만 한다.
- **사이드바(`shared/layout/navigation.ts`)가 화면 목록의 정본이다.** 아직 없는
  화면은 `pending` 으로 두면 라우터가 stub 을 만든다 — 제목·단계·설명을 두 곳에
  적지 않는다.
- **역할 판정은 `shared/auth/roles.ts` 한 곳에서** 한다. 갈라지면 어떤 화면은
  단추를 보이고 어떤 화면은 안 보이는 상태가 되고, 그 차이는 아무도 설명할 수 없다.
  이것은 **표시일 뿐 권한이 아니다** — 권한은 서버가 판정한다.
- **모름을 됨과 섞지 않는다.** 검색 결과의 `unknown` 은 `match` 와 다른 배지를
  단다. 같게 보이면 사람은 헛걸음을 하고, 그 한 번으로 시스템 전체가 안 믿긴다.
- **사람이 보는 글자는 격식 있는 용어로**, 코드·주석·API 값은 개발자 말 그대로. 화면의 열 이름·
  단추·배지·탭·메뉴: 참조(쓰임 ×) · 필터 해제(거르기 풀기 ×) · 선택(고르기 ×) · 추가(더하기 ×) ·
  삭제·참조 해제·이동(내리기·떼기·옮기기 ×) · 미지정(안 정함 ×) · 미반영(뒤짐 ×) · 준비 중(미구현 ×) ·
  처리 필요 항목(남은 일 ×) · 검토함은 미처리 / 의견 있음 / 보류됨 / 결정 완료 / 대상 삭제됨, 동작은
  의견 제출·수정·철회 / 보류 / 재검토 / 직접 선택 · 가능 계열·측정 물성·수행 불가(되는·얻는·못 하는 ×) ·
  기종 고유 사양(이 기종만의 사양 ×) · 시험 항목 미지정(항목 미정 ×). 「검색축 → 검색 조건」 과 같은
  규칙이다: 도메인 전문가에게 「쓰임」「거르기」 는 개발자 말이다(2026-09-16 운영 첫 사용 피드백).
  2026-09-24 전면 정리에서 더한 것 — **관형형(-는·-한·-된)으로 이름을 짓지 않는다**:
  적용 시험 항목(쓰는 × · 구성 ×) · 수행 가능 장비/계열·기종(가능한 장비 ×) · 적용 규격(이 시험의 규격 ×) ·
  적용 분류(붙는 분류 ×) · 별칭(다른 표기 ×) · 기타 항목·기타 속성·기타 사항(그 밖의 … ×) ·
  표시 단위(읽는 단위 ×) · 작업·수행자·변경 내용(한 일·누가·바뀐 것 ×) · 외형(생김새 ×) ·
  접두어(앞자리 ×) · 자동 제안 연결(기계가 제안한 연결 ×) · 이미지 첨부(그림 넣기 ×) ·
  제거(빼기 ×) · 연결(잇기·이어 주기 ×) · 지정(정하기 ×) · 변경(바꾸기 ×) · 초기화(비우기 ×) ·
  활성화/비활성화(켜기·끄기 ×) · 값 입력(채우기 ×) · 전체 선택(전부 넣기 ×) · 바로 가기(보러 가기 ×).
  **첨부는 「이미지」, 지식 그래프는 「그래프」** — 둘 다 「그림」 이라고 부르면 다른 것인 줄 안다.
  단추의 동사형을 전부 없애지는 않는다: 「닫기」「찾기」「보기」「가져오기」「내보내기」「되돌리기」
  「합치기」 는 한국어 UI 의 정석이라 명사로 바꾸면 오히려 어색하다.
  **시스템 메시지는 하십시오체다**(~하세요 ×) — 사내 시험 플랫폼의 격에 맞춘다. 오류 문구·안내문·
  빈 상태가 모두 해당한다. 화면·MCP·의미 색인 카드가 **같은 말을 써야 한다**: 화면이 「수행 가능
  장비」 라고 하는데 AI 가 「가능한 장비」 라고 답하면 사람은 둘을 다른 것으로 읽는다.
  **새 기록을 만드는 단추는 어느 화면이든 「<대상> 등록」** — 계열 등록 · 기종 등록 · 시험법 등록 ·
  장비 등록 · 신뢰성 시험 등록 · 부서 등록 · 검색 조건 등록 · 사양 등록 · 속성 등록 · 교정 기록 등록 ·
  기준정보는 「<축 이름> 등록」(시험 항목 등록·거점 등록). 있는 것에 **잇거나 목록에 줄을 더하는**
  단추만 「<대상> 추가」(시험 항목 추가 · 표기 추가 · 멤버 추가 · 줄 추가). 전에는 「추가」「값 추가」
  「조건 추가」 가 섞여 있어 같은 일인지 이름만 보고는 몰랐다(2026-09-18).
- **빈 목록은 이유를 말한다**(`EmptyState`). "없습니다" 만 적으면 데이터가 없는
  것인지, 거르기가 좁은 것인지, 권한이 없는 것인지 구별할 수 없다.

- **고를 것이 스물을 넘으면 `<Select>` 를 쓰지 않는다**
  (`shared/components/SearchablePicker`). 시험 항목 87종·사양 정의 209종을 통째로
  펼쳐 놓고 눈으로 찾으라고 하면 그 일은 실패하고, **못 찾은 사람은 없다고 결론
  내리고 새로 만든다** — 그러면 같은 값이 둘로 갈린다. 닫힌 축에서는 특히 나쁘다.
  피커는 두 길을 함께 낸다: 치는 것(이름의 일부를 알 때)과 「상세」(무엇이 있는지
  모를 때). 이미 고를 수 없는 줄은 **이유를 badge 에 적는다** — 비활성만 시키고
  말 안 하면 버그로 읽힌다.
- **시험 항목이 카탈로그의 가운데다**(`/catalog/test-items`). 계열·기종·규격·물성 네 화면이
  전부 「→ 시험 항목」 으로 향하므로 시험 항목에서 출발하는 화면이 있어야 공백이 보인다 —
  한 줄에 얻는 물성·규격·되는 계열/기종·보유 대수·검색축, 0 이 곧 공백. 사이드바 위쪽
  「신뢰성 시험」 과는 다른 층이다 — 아래 항목.
- **「신뢰성 시험」 과 「시험 항목」 은 다른 층이다**(`modules/reliability`). 시험 항목은 장비가
  할 수 있는 **측정**(인장·경도·열충격)이고 전사 공용 정의라 카탈로그에 산다. 신뢰성 시험은
  부서가 제품 개발·검증을 위해 **수행하는 시험**(고온고습 1000h · 열충격 500 cycle)이고
  **부서가 등록한다**(그 부서 관리자 · 시스템 관리자). 신뢰성 시험 하나가 시험 항목 하나
  이상을 써서 돌고 그 항목이 장비로 이어지므로, 사슬은 기존 것 위에 한 층이 얹힌다 —
  아래 사슬은 건드리지 않는다. 사이드바 「신뢰성 시험」 아래에는 「부서 정보」 에서 체크한
  부서(`reliability_listed`)만 선다: 조직도를 통째로 펼치면 시험을 하는 부서 넷을 찾으려고
  서른을 훑는다. 고정 칸은 이름·목적·시험 항목뿐이고 **나머지는 「항목」 으로 붙인다** —
  아래 항목.
- **기계 자격으로 들어온 신뢰성 시험은 후보다**(ADR 0009). PAT 로 들어온 쓰기는
  `status="candidate"` 로 서고, 사람이 화면에서 읽고 확인해야 확정이다 — 조건 속성 한 칸이
  그대로 장비 판정이 되고 그 장비로 보고서가 나가는데, AI 가 채운 칸은 **빈 칸과 달리 눈에
  안 띈다.** 가르는 것은 「누가」 가 아니라 **「어느 통로」** 다(`request_context.get_actor_token`)
  — PAT 의 소유자도 사람이라 `created_by` 로는 못 가른다. **확정된 줄은 기계가 못 고친다**
  (칸도, 그림도, 지우는 것도): 판정은 `reliability.require_editable()` 한 곳이고 첨부도 그것을
  부른다 — 내용을 고치는 길이 둘인데 한 쪽만 막으면 나머지가 열려 있다. 확인과 「다시 후보로」
  는 **사람만** 한다(MCP 에 도구를 안 만드는 것으로는 부족하다 — PAT 는 경로를 직접 부른다).
  전사 목록에는 확정된 것만 내고, 후보는 그 부서 화면에서 본다.
- **항목 정의 — 칸을 열이 아니라 행으로 둔다**(`modules/attributes`). 신뢰성 시험(뒤에는
  보유 장비)에 무슨 칸이 붙어야 하는지는 조사 중이다. 열을 미리 뚫으면 안 쓰는 열이 남고
  이름을 바꾸려면 마이그레이션이 필요하므로, 항목의 이름·종류·단위를 `attribute_definitions`
  행으로 두고 값(`attribute_values`)이 그 행을 가리킨다 — 기종 사양(`SpecDefinition`)과 같은
  얼개. 정의는 **초안(draft)** 과 **정식(standard)** 으로 나뉜다: 초안은 값을 적는 사람이
  목록에 없는 이름을 쓰면 자동으로 생기고, 정식은 시스템 관리자가 「공통 → 항목 정의」 에서
  올린다. **초안은 표시와 수집만 한다** — 검색 판정·resolve·의미 색인 카드·MCP 구조 응답에는
  정식만 들어간다(초안이 쌓여도 검색 품질이 흔들리지 않게). 이름이 갈리는 것(온도·시험온도·
  Temp)은 입력 칸이 정식 → 초안(건수) 순으로 먼저 보여 줘서 줄이고, 그래도 갈린 것은
  `merge` 로 합친다(종류가 같을 때만; 값의 `definition_id` 가 바뀌고 원래 행은 꺼진다).
  값은 종류별로 칸을 나눠 저장하고(`300` 과 `약 300` 이 한 칸에 섞이지 않게) **단위는 값에
  남긴다** — 초안은 사람마다 단위가 다를 수 있으니 저장은 적힌 대로, 환산은 읽을 때.
  종류 `condition` 은 검색 조건 축(`condition_key_id`)을 갖는다: 같은 물리량을 같은 축·
  차원으로 적어야 나중에 「이 절차를 돌릴 수 있는 장비」 판정이 성립한다. 화면은 친 글자의
  모양으로 종류를 알아맞힌다(`85℃` → 수치, `-40~125` → 구간) — 종류를 고르라고 하면 전부
  문장으로 넣기 때문. 필수 항목이 비면 **말하되 막지 않는다**(막으면 아무 값이나 넣는다).
  사람이 읽는 한 줄(`display`)은 서버가 만든다(`shared/attribute_text.py`) — 화면·MCP·
  색인 카드가 같은 글자를 쓰게. **화면의 말은 「속성」**(사이드바 「신뢰성 시험 속성」 ·
  「보유 장비 속성」, 등록 창의 「속성」 섹션) — 「항목」 은 「시험 항목」 때문에 이미 무거운
  말이라 등록 창에서 두 층이 한 낱말로 읽힌다. 「공통」 의 정의 메뉴 넷은 전부 **「<대상> <칸
  종류>」** 꼴이다 — 시험 항목 검색 조건 · 장비 기종 사양 · 신뢰성 시험 속성 · 보유 장비 속성.
  전에는 「시험 조건 정의」 「장비 사양 정의」 라서 어느 객체에 붙는지 이름만 보고는 몰랐다. 코드·표·API 는 attribute 다. 보유 장비도
- **속성 값은 되찾을 수 있어야 한다**(`attributes/filters.py`). 목록 넷(보유 장비·계열·규격·
  신뢰성 시험)이 `attr` 을 여러 번 받고 **모두** 만족하는 것만 남긴다(`attr=invest_year>=2020`
  · `attr=purpose~고온` · `attr=book_url*`). 열이 아니라 행으로 쌓은 값이 상세 화면에만 뜨면
  사람은 목록을 내려받아 엑셀에서 거르고, 그때 이 시스템은 입력 창구일 뿐이다. 왼쪽은 정의의
  `key` 이고 **주소에 그대로 실리므로** 영문·숫자·`_.-` 만 받는다(`_check_key_shape`) — 한글
  key 를 허용하면 그 속성은 영영 못 거르는 칸이 된다. 모르는 key·못 읽는 꼴은 **빈 답이 아니라
  400** 이다: 빈 답이면 사람은 값이 안 적힌 줄 안다. 수치·범위는 값마다 단위가 달라(kN·N·kgf)
  SQL 로 못 재므로 그 정의의 값 행만 읽어 **환산해 파이썬에서** 판정하고, 범위는 「닿나」 로
  읽는다(`>=100` 은 위쪽 끝, `<=-20` 은 아래쪽 끝, 빈 끝은 제한 없음). 화면 칸은
  `attributes/AttributeFilterBar` 하나를 목록 넷이 함께 쓴다.
- **신뢰성 시험의 조건 속성은 그대로 장비 판정이 된다**(`reliability/capability.py`,
  `GET /reliability-tests/{id}/equipment`, 화면은 「가능한 장비」 단추). 조건 속성
  (`kind="condition"`)은 검색축과 **같은 축·같은 차원**으로 적히므로 `ConditionQuery` 로 옮겨
  기존 장비 찾기에 넘긴다 — **판정 규칙을 두 벌로 만들지 않는다**: 두 벌이면 같은 장비를 놓고
  검색 화면과 이 화면이 다른 답을 한다. 범위 하나는 **물음 둘**이다(위로 얼마까지 · 아래로
  얼마까지) — 하나로 묶으면 125 까지 올라가지만 0 까지밖에 안 내려가는 챔버가 통과한다.
  단위를 축의 SI 로 못 바꾸는 조건은 빼고 `skipped` 에 이유를 적는다(조용히 빼면 조건을 다 본
  것처럼 「가능」 으로 읽힌다). 답에는 `unmet_count` 가 함께 온다 — 「0대」 가 「그 시험 항목이
  적힌 장비가 없어서」 인지 「조건이 안 맞아서」 인지를 그것이 가른다. 부서로 좁히지 않는다.
  같은 편집기를 쓴다(등록 창 「속성」 섹션 · 상세의 「속성」 탭, `attributes` 필드). 고정 칸의
  기준은 「코드가 참조하는가」 + 「모든 대상에 항상 있어 아무도 끄면 안 되는가」 — 부서·
  이름·목적·시험 항목이 그렇고, 조건·규격·판정 기준·시료 수는 속성이다.
- **기준정보는 한 화면이다**(`modules/reference`, 사이드바 「공통 → 기준정보」 → `/reference`).
  왼쪽에 객체 종류 전부가 세 층(카탈로그 · 사내 운영 · 이름 사전)으로 서고, 고르면 오른쪽에
  그 종류의 판 — 이름 사전의 축은 값 목록 + 축 편집·값 등록·값 편집(`AxisPanel`, 관리자만
  쓰기), 자기 표의 객체는 건수 · 고정 칸 · 관리자가 정의한 칸(검색 조건·사양·속성)과 목록·
  정의 화면으로 가는 문. 「기준정보 편집」 메뉴는 없어졌다(`/admin/vocabulary` · `/vocabulary`
  는 허브로 넘어간다). **정의 화면 여섯(시험 항목 검색 조건 · 장비 기종 사양 · 속성 넷)도
  사이드바에 없다** — 기준정보 안의 문이고 「← 기준정보」 로 돌아온다. 전에는 「기준정보」 가
  이름 사전만 뜻해서 계열·기종·규격은 기준정보가
  아닌 것처럼 읽혔고, 종류마다 무슨 칸을 더할 수 있는지가 화면 넷에 흩어져 있었다(ReportArchive 는 객체 종류·속성 정의·
  별칭·관계를 한 체계로 둔다). **저장은 안 바꿨다** — 사슬의 뼈대(계열·기종·규격·보유 장비)는
  자기 표, 이름은 축의 값. 판정(사양 → 조건 축 → 장비)이 FK 로 타야 빠르다. 바뀐 것은 관리가
  한 체계로 보이는 것이고, 그래서 **모든 객체 종류에 관리자가 칸을 더할 수 있다** — 속성의
  대상이 계열·시험법까지(`attribute_values.series_id · method_id`; 값이 가리키는 규격은
  `ref_method_id`). 새 객체 종류를 더하면 `reference/services._TABLE_KINDS` 에 한 줄.
  **보유 장비의 정식 속성 셋은 설치가 심는다**(`vocabulary/reference.EQUIPMENT_ATTRIBUTES`:
  장비 용도 · 투자 연도 · 장비 예약 URL). 현장 장비 목록의 열(중분류·소분류·장비명·장비 용도·
  보유처·건물·설치 위치·담당자·자산번호·투자년도·예약 URL) 중 나머지는 고정 칸이 이미 갖는다
  — 중분류·소분류 = 장비 분류의 군·유형, 보유처 = 보유 부서, 건물 = 거점. 겹치는 칸을 속성으로
  또 만들지 않는다: 같은 사실이 두 곳에 있으면 한쪽만 고쳐진다. key 로 찾아 없을 때만 심으므로
  관리자가 끄거나 이름을 바꾼 것을 설치가 되돌리지 않는다.
  **검색축**(`test_item_condition_keys`)은 시험 항목마다 뜻이 있는 조건(인장 → 하중·속도·
  온도)이며 카탈로그가 갖고 있지 않은 지식이라 사람이 정한다; `seed_test_item_axes.py` 가
  논쟁의 여지 없는 것만 첫 값으로 심는다 — **더하기만** 한다(적힌 짝이 없으면 더하고,
  사람이 화면에서 더한 축은 안 뺀다). 축이 늘어난 릴리스 뒤에 다시 돌리면 그 축이 붙는다.
- **분류는 두 층이고, 군을 고르면 아래 유형이 다 걸린다**(`equipment/category_tree.py`).
  계열은 유형을 가리키므로 군을 가리키는 계열은 없다 — 「계열 없는 분류」 를 세면 군 16종이
  공백처럼 보이지만 그것은 트리의 윗층이다. 거르기는 `family`(자기 + 후손)로 비교하고,
  선택지에는 군을 **아래 수를 합쳐** 올린다(`rollup`, `FilterOption.detail` 에 「묶음 · N개
  분류」 / 어느 군인지). 유형이면서 계열이 없는 분류는 온톨로지가 아는 장비 종류라 지우지
  않는다 — 반입이 다시 만들고, 손으로 계열을 세울 자리다.
- **상세에는 옆 목록을 둔다**(`shared/layout/SidePanel` + `RecordListPanel`). 장비·기종·계열은
  **여러 개를 견주는 것이 일**이라(같은 계열의 기종 사양이 어디서 갈리나, 한 부서 장비를
  훑기), 옆 것을 보는 길이 브라우저 뒤로 가기뿐이면 보던 자리와 걸어 둔 거르기가 매번
  사라진다. 목록은 **사이드바 옆 껍데기 층**에 붙는다 — 본문 안에 두면 본문 여백에 갇히고
  본문과 함께 스크롤된다. 여닫는 상태는 껍데기가 갖고(단추가 상단 바에 있어야 하므로),
  내용은 화면이 포털로 넣는다. 그 목록도 **거르기는 서버가 한다**.
- **모달은 머리글로 옮길 수 있다**(`ui/dialog.tsx`). 머리글이 손잡이고, 바닥글은
  붙박이다 — 둘 다 프리미티브가 해 주므로 모달마다 적지 않는다. 다만 그 판별은
  `DialogHeader`·`DialogFooter` 를 **찾아야** 도는데, 등록 모달은 대부분 `<form>` 이나
  조각으로 감싼다. 그래서 한 겹과 조각을 들여다본다 — **안 들여다보면 조용히 안
  걸리고, 안 걸린다는 사실은 내용이 길어지기 전까지 안 보인다**(실측: 등록 모달
  넷이 전부 그랬다).
- 모달을 옮기는 것은 **여백**이지 `transform` 이 아니다. 인라인 transform 은 여는
  애니메이션을 덮어 죽인다.
- **지식 그래프는 읽기 전용이고, 큰 데이터를 통째로 안 준다**(`modules/graph`, 사이드바
  「공통 → 지식 그래프」 → `/graph`; StandardPlatform 의 것을 옮겨 왔다). 「구조」 는 종류와
  선 종류만 그린다(객체가 십만이어도 답은 종류 수 노드) · 「탐색」 은 시작점을 찾거나 종류를
  훑어 고른 뒤 이웃을 **서버 상한 안에서**(depth ≤ 6 · fanout ≤ 500 · nodes ≤ 20,000) 든다.
  잘린 노드는 `degree` 와 `truncated` 를 같이 받아 「+N」 배지를 달고 상세 판이 「여기서
  확장」 을 낸다 — **잘랐으면 잘랐다고 말한다**. 노드 id 는 `"<종류>:<uuid>"` — 표가 다르면
  uuid 만으로는 종류를 모른다. **관계의 정본은 `graph/model.py` 하나다**: 새 선 종류는
  거기 `EdgeKind` 한 줄(원본·대상 종류 · `Select` 문 · 이름과 역방향 이름)이고 overview ·
  neighborhood · subgraph · node 상세가 전부 그 목록을 돈다 — 화면·조회마다 관계를
  따로 적으면 「그림에는 있는데 상세에는 없는」 선이 생긴다. 장비는 `visible_equipment_ids`
  로 걸러 **가린 부서의 장비는 검색·이웃·목록·구조 수 어디에도 없다**. 이어 놓고 끊는
  것은 각 객체 화면이 한다(그림에서 선을 지우는 쓰기가 들어가면 상세와 어긋난다). 객체
  상세(계열·시험법·장비·시험 항목)에는 `GraphPanel` 이 그 노드의 1단계 이웃을 작은 그림으로
  보이고, 「지식 그래프에서 넓게」 로 `/graph?focus=<id>` 에 간다. 사케이 「흐름」 만 plotly
  를 쓰며 `LazyPlot` 이 `import()` 로 따로 받는다 — 정적으로 넣으면 한 번도 안 여는
  사람까지 첫 화면에서 1MB 를 받는다.

## 스크립트와 배포

- `.ps1` 은 **UTF-8 BOM** 으로 저장한다. Windows PowerShell 5.1 이 BOM 없는
  스크립트를 CP949 로 읽어 한글이 깨지고 구문 오류가 난다. 시험이 검사한다
  (`tests/architecture/test_scripts.py`).
- `.env` 는 **BOM 없이** 저장한다. BOM 이 붙으면 **첫 줄 키만 조용히 무시된다** —
  `APP_ENV` 가 기본값으로 떨어져 운영이 development 로 뜬다.
- **네이티브 명령은 `Invoke-Native` 로 감싼다.** 5.1 은 stderr 한 줄을 종료성
  오류로 바꾸는데, alembic 은 INFO 를 stderr 로 낸다 — 감싸지 않으면 정상 배포가
  실패로 뒤집힌다. 판정은 종료 코드로만 한다. **넘기는 블록 안에서 `$command`·
  `$FailureMessage` 라는 이름을 쓰지 않는다** — 매개변수와 같은 이름이라 블록이 실행되는 순간
  블록 자신으로 풀리고, exe 에는 `-encodedCommand` 로 간다(운영 첫 설치에서 WinSW 가 그렇게
  전부 죽었다). 시험이 검사한다.
- **성공한 스크립트는 `exit 0` 으로 끝낸다.** 스크립트의 종료 코드는 마지막
  네이티브 명령의 것을 물려받는데, robocopy 는 성공해도 0 이 아니다(비트 플래그).
  그대로 두면 작업 스케줄러가 매일 백업을 실패로 기록하고, 사람은 곧 그 알림을
  무시하게 된다 — 그러면 진짜 실패한 날에도 아무도 안 본다.
- **요청 밖에서 시간이 걸리는 일은 작업 큐로**(`app/jobs/` · `run_worker.py` · 서비스
  `TestScope-Worker`). 브로커 없이 `jobs` 표 + `SELECT … FOR UPDATE SKIP LOCKED` — ReportArchive·
  MatNexus 와 같은 방식이고, 폐쇄망에 Redis 를 반입하지 않는다. 핸들러는 `@handlers.handler(kind)`
  로 등록하고 `handlers.load_all()` 에 모듈 한 줄을 더한다; 종류 이름은 `kinds.py` 한 곳(오타가
  나면 조용히 `failed` 로만 쌓인다). 핸들러는 예외를 삼키지 않는다 — 워커가 잡아 백오프로 재시도하고
  한도를 넘기면 `failed` 로 남긴다. 워커는 `app.all_models` 를 먼저 읽는다(안 읽으면 핸들러가
  모르는 표를 외래키로 가리키는 순간 첫 flush 에서 죽는다). 주기 작업은 `schedule.PERIODIC` 한 줄 —
  시각을 메모리에 들지 않고 큐에 마지막으로 넣은 시각을 본다.
- **의미 검색은 선택 부품이다**(`shared/embeddings.py` · `shared/semantic.py`). 엔진(Ollama
  bge-m3) · 확장(pgvector) · 표(`search_chunks`) 셋이 다 있어야 켜지고, 하나라도 없으면 검색은
  이름·별칭으로만 돈다 — **예외를 내지 않는다.** `EMBEDDING_BACKEND` 는 `off`(기본) · `mock`(해시
  벡터, 시험용 — 뜻은 없다) · `ollama`. 표는 마이그레이션이 아니라 `semantic.ensure_schema()` 가
  만든다(확장이 없는 서버에서 `alembic upgrade` 가 통째로 서면 안 된다) — `Base.metadata` 에 없고
  `migrations/env.py` 가 autogenerate 에서 뺀다. 카드 단위로 건다: 객체 하나(시험 항목·물성·계열·
  기종·규격·보유 장비·신뢰성 시험) = 카드 한 장, 이름만이 아니라 별칭·소개·이어진 것을 적는다.
  결과는 **후보**다 — `resolve` 의 4단계(`뜻이 가까움`, 유사도 0.45 아래는 버림)와
  `/search/semantic` 이 주고, 장비는 여전히 사슬(시험 항목 → 조건 → 장비)이 답한다. 보유 장비
  카드는 `visible_equipment_ids` 로 거른다. 차원이 모델과 다르면 거절한다(섞이면 순위가 조용히
  엉뚱해진다). pgvector 의 Windows 산출물은 `scripts/deploy/pgvector/pg<판>` 에 커밋한다
  (300KB, `build_pgvector.ps1` 로 재현) — PG 메이저마다 따로.
- **운영은 Windows 서비스로 돈다**(`service.ps1` · WinSW). 콘솔로 띄운 앱은 재부팅한 날
  아침에 화면이 안 열리고, 그 사실은 그때야 드러난다. 서비스 정의는 **앱 폴더 밖**
  `<AppPath>_service\` 에 둔다 — 배포가 앱 폴더를 통째로 `_prev` 로 옮기므로 안에 있으면
  배포마다 깨진 경로를 가리킨다. 서비스가 띄우는 것은 run_server.ps1 이 아니라 venv 의
  python.exe 가 직접 run.py 다(PowerShell 을 사이에 두면 멈출 때 자식이 남고, 다음 기동이
  「포트에 이미 응답하는 서버」 로 거절된다). WinSW 는 저장소에 넣지 않고 **패키징이 판과
  sha256 을 못 박아 받는다** — 18MB 바이너리를 git 에 두지 않으면서 폐쇄망에 다른
  바이너리가 들어가지 않게. WinSW 는 XML 을 서비스 시작 때 읽으므로 이미 등록된 서비스는
  **XML 만 다시 쓰고 시작한다** — 지웠다 다시 만들면 SCM 의 「삭제 표시」·남은 핸들에 uninstall 이
  -1 로 죽는다(운영 첫 설치 실측). 표시 이름·의존성 같은 SCM 속성을 바꿀 때만 uninstall 뒤 install. `deploy.ps1`·
  `rollback.ps1` 은 서비스를 **스스로 멈추고 올린다**(MCP 먼저 멈추고 백엔드 먼저 올림);
  staging 검사에서 멈추면 서비스도 되살리고, 마이그레이션이 실패하면 멈춘 채 둔다.
  시험이 검사한다(`tests/architecture/test_scripts.py`).
- **배포 스크립트는 패키지에 함께 담는다**(`package_deploy.ps1`). 서버가 릴리스만
  받는 환경이어도 zip 하나로 그다음 배포가 돌아야 한다 — 빠뜨리면 첫 배포에
  저장소를 클론하는 수밖에 없고, 폐쇄망에는 그 길이 없다. 시험이 검사한다.
- **wheel 은 Python 마이너 버전에 묶인다.** 패키징은 `.python-version` 을 읽어 py
  런처로 그 버전을 집는다 — PATH 를 믿으면 서버가 거절할 패키지가 조용히 만들어지고,
  그 사실은 배포하는 자리에서야 드러난다.
- **반입은 단계마다 한 모듈이다**(`scripts/catalog_import/`). 진입 `import_catalog.py` 는
  순서와 요약만 갖고, 단계의 상태(`terms._CODE_CLASHES` · `values._FREE_MADE`)는 **모듈로
  읽는다** — 이름을 가져오면 임포트 시점의 빈 값을 든다. 새 단계는 모듈 하나를 더하고 진입에
  한 줄 잇는다. 갈라 놓은 것이 같은지는 `--dry-run` 요약을 전후로 견줘 본다.
- **행은 마이그레이션에 넣지 않는다.** 기준정보 축·조건 정의·사양 그룹·사양 정의는
  `app/modules/vocabulary/reference.py` 가 심고, 설치(`seed_install.py`)와
  배포(`seed_reference.py`)가 그것을 부른다. 마이그레이션에 넣으면 모델로 표를
  만드는 시험이 그 행을 못 받아, 같은 목록을 시험 쪽에 한 벌 더 적게 된다.
- **반입이 못 정한 것은 검토함으로 간다**(`review/`, 「관리 → 검토함」). 어느 시험의 규격인가 ·
  무슨 조건을 묻나 · 이 물성이 나오나 · 이 사양을 정의로 올리나 — 개발자가 정하지 않고 **후보·
  추천·근거**를 세워 도메인 전문가가 고른다. 추천은 정본(`source/catalog/proposals/<큐>.json`)에
  **확실한 것만** 적고, 확신이 없으면 `hint` 만 적는다(왜 추천이 없는지가 화면에 보인다). 화면은
  추천을 자동으로 고르지 않고 근거를 옆에 붙이며 「직접 고르기」 와 「건너뛰기」 를 늘 둔다.
  **의견과 확정을 가른다**: 의견(`ReviewVote`)은 로그인한 누구나 내고 데이터를 안 건드리며
  모이기만 한다 — 그래서 메뉴가 「관리」 가 아니라 「공통」 에 있다; 확정은 시스템 관리자가 하고
  그때 **기존 규칙**(`promote_pending` · `free_specs.promote` …)이 돈다 — 검토함이 따로 규칙을
  갖지 않는다. 의견 없이도 확정할 수 있다(사람이 적을 때 병목이 안 되게). 확정 칸은 다수 의견으로
  미리 채우되 자동 확정은 없다. 누가 골랐고 추천을 따랐는지(`followed`)가 남고, `export_review.py` 가 결정을
  정본에 되돌려 써서 운영 반입이 다시 묻지 않고 적용한다. 정한 것은 **기록으로 남고**(누가·
  언제·추천대로였는지) 「다시 열기」 로 다른 걸로 고칠 수 있되 이미 일어난 일(지운 연결·만든
  정의)은 안 되돌린다 — 그건 원래 화면에서. 대상이 지워진 줄은 「대상 없어짐」 으로 따로
  닫는다: 정한 것과 섞이면 셈이 틀린다. 새 물음을 더하려면 `QUEUES` 에 한 줄, `_refresh_*` 와
  `_apply` 에 한 가지씩.
- **이 설치에서 생기는 물음은 정본과 주고받지 않는다**(`Queue.local`). 「초안 속성 정리」 가
  그렇다: 초안 속성은 값을 적는 사람이 새 이름을 쓰면 생기고 자동 key 는 설치마다 다른
  난수라, 정본에 내보내면 다른 설치에서 아무것도 안 가리키는 결정이 쌓인다. 그래서 후보를
  파일이 아니라 **지금 이 설치의 상태**에서 세우고(`_refresh_attribute_drafts`),
  `export_decisions` 는 건너뛴다. 값이 0건인 초안은 아예 안 묻는다 — 정의 화면에서 지우면
  그만인 것을 물음으로 세우면 아무 일도 안 하는 줄이 검토함을 채운다. 추천은 **띄어쓰기·
  대소문자를 지운 이름이 같은 속성이 딱 하나일 때만** 붙인다(「시험 온도」/「시험온도」):
  한쪽이 다른 쪽에 들어가는 이름(「온도」/「보관 온도」)은 후보로만 올린다 — 첫 보기를
  습관적으로 누르게 두면 추천이 곧 오답이 된다. 줄마다 붙는 곳이 달라 링크도 다르므로
  `payload.link` 가 큐의 서식을 덮는다.
- **알림의 규칙은 `notifications/rules.py` 한 곳이다.** 만드는 것은 `services.notify()`
  하나이고, 언제 누구에게 가는지는 규칙 모듈이 정한다 — 라우트마다 손으로 만들면 링크를
  빼먹은 알림이 생기고, 그것은 읽고 끝나는 글이 된다. 날이 되면 생기는 알림(교정 만료)은
  스케줄러가 없으니 **읽는 요청이 그 사람 몫만 훑고**(10분에 한 번), 같은 일로 두 번 안
  울리게 `dedupe_key` 를 단다(사람마다 유일). 받는 사람은 그 일을 **할 수 있는** 사람이다
  — 승인은 시스템 관리자, 장비 상태·교정은 소유 부서의 관리자, 바꾼 사람 본인은 뺀다.
  읽을 수 없는 사람에게는 안 보낸다(거절된 계정은 로그인을 못 한다).
- **카탈로그 신선도는 지문으로 잰다**(`server/catalog_state.py`). 반입은 사람이 돌리는데
  안 돌린 사실이 어디에도 안 적히면 「카탈로그에 없던데」 가 반입을 안 한 것인지 정본에도
  없는 것인지 구별되지 않는다. 반입이 끝에 정본(반입이 읽는 파일만 — `equipment/` ·
  `ontology/`)의 sha256 을 `catalog.imported` 감사 기록에 남기고, 서버 상태와 관리자의
  「남은 일」 이 파일의 지문과 견줘 「뒤짐」 을 말한다. 시각이 아니라 내용이다 — 운영엔 git
  이 없고, 다시 빌드만 한 파일은 내용이 같다. `urls.json`·`graph.json` 은 지문에 안 넣는다:
  PDF 하나 더 받은 것을 「뒤짐」 으로 읽으면 그 경고는 곧 무시된다.
- **있는 정의는 시드가 따라잡되, 빈 것만 채운다**(`converge_spec_definitions`). 사양 정의는
  세 길(기준정보 · 카탈로그 손 정의 · 온톨로지 승격)로 생기므로 정의 → 검색축 연결은
  `SPEC_DEFINITION_LINKS` **한 표**가 정하고, 튜플에 적힌 축은 그 표와 같아야 한다(시험이
  검사). 표를 고쳐도 이미 만들어진 정의는 안 따라오니 시드가 **연결이 빈 정의만** 잇고,
  사람이 화면에서 다른 축으로 바꿔 둔 것은 그대로 둔다. 종류 바꾸기는 문장 → 구간 한
  방향뿐이다(`SPEC_KIND_UPGRADES`): 경도계 하중 「500 · 750 · 1000」 은 양끝을 구간에,
  목록은 비고에 남긴다 — 문장인 채로는 축에 못 이어 검색이 「모름」 으로 답했다.
  **차원이 같다고 잇지 않는다** — 공급 전압은 전원 사양이지 시험 능력이 아니다.

## AI 표면 (MCP)

- **MCP 서버는 얇은 프록시다.** `mcp_server/` 는 REST 를 부르기만 하고 DB 를 안 읽는다.
  로직이 두 벌이 되면 화면과 AI 가 다른 답을 하고, 그때 어느 쪽이 맞는지 알 방법이 없다.
  필요한 계산이 생기면 **백엔드에 엔드포인트를 만든다.**
- **만능 토큰을 두지 않는다.** 서버는 받은 `Authorization` 을 나르기만 한다.
- **막는 자리가 둘이다 — 범위와 자격.** 범위는 「이 토큰이 무엇을 건드리나」, 자격은
  「이 사람이 해도 되나」. 둘 다 403 으로 오지만 할 일이 다르다: `TSC-AUTH-0106` 은 토큰을
  다시 만들면 되고, `TSC-AUTH-0103`·`TSC-WORKSPACES-0003` 은 다시 만들어도 소용없다.
  **그 구별을 문서와 도구 설명이 말해야 한다** — 아니면 사람이 토큰만 거듭 재발급한다.
  자격은 셋으로 갈린다: **부서 멤버**(제 부서의 보유 장비를 넣고 고침 · 열린 축에 값 더하기)
  · **부서 관리자**(+ 장비 지우기 · 신뢰성 시험 · 멤버 관리) · **시스템 관리자**(+ 카탈로그 ·
  규격 · 닫힌 축 · 기준정보 고치기). `permissions.py` 의 `role="member"` 가 그 갈림이고,
  **기본값은 manager 다** — 자격이 넓어지는 일은 부르는 자리에 보여야 한다.
- **값을 더하는 것과 고치는 것은 자격이 다르다.** 더하는 것은 늘어날 뿐이라 사후 병합으로
  풀 수 있지만, 이름을 고치는 것은 그 값을 쓰는 모든 화면의 글자를 바꾸는 일이라 되돌릴
  자리가 감사 기록뿐이다. 그래서 열린 축에 값은 누구나 더하고, `update_term`·`merge_terms`·
  축 만들기는 시스템 관리자다.
- **기계 자격은 범위로 막는다**(`PersonalAccessToken.scopes`). 판정은
  `shared/auth.py` 한 곳이고, 표에 없는 경로는 못 쓴다 — 새 엔드포인트가 생길 때마다
  자동으로 열리면 그것을 알아채는 사람이 아무도 없다. **POST 지만 읽기인 것**
  (`/api/resolve` · `/api/search/`)은 예외 목록에 적는다.
- **만들기 전에 찾게 한다.** 이름으로 쓰는 API 는 `resolve` 로 해석하고, 하나로
  안 정해지면 **거절하고 후보를 준다.** 비슷한 것을 골라 주면 그 선택은 아무 데도
  안 남는다. **새 객체 종류를 만들면 `resolve` 에도 한 줄 더한다** — 목록으로 찾게 두면
  AI 는 첫 줄을 집고, 틀린 줄도 첫 줄이면 집는다. 지금 아는 것: 계열·기종·기준정보 값·
  규격·신뢰성 시험·보유 장비·부서.
- **부서가 가진 것은 이름이 겹친다.** 「고온고습 1000h」 는 거의 모든 부서에 하나씩 있으므로
  이름이 정확히 같아도 **둘 이상이면 exact 가 아니다**(`workspace` 로 좁히면 하나가 된다).
  장비는 자산번호만 exact 이고 이름은 대개 후보다. 그리고 resolve 는 목록과 **같은 가시성
  규칙**을 쓴다 — 아니면 못 보는 장비의 id 가 후보로 흘러나오고, 그 id 로 부른 다음 요청이
  404 로 끝난다.
- **행동 규칙은 API 설명에 적는다.** MCP 도구 설명이 거기서 나온다 — AI 가 도구를
  볼 때 항상 함께 오는 유일한 글이다. 별도 지침 문서보다 낫다.
- 새 도구를 더하면 `tests/architecture/test_mcp_tools.py` 가 반환 표기와 설명을
  검사한다. **목록은 `_listed()` 로 감싼다** — 배열을 그대로 돌려주면 그 도구는
  통째로 죽고, HTTP 로는 멀쩡해서 원인이 안 보인다.
- **MCP 도구는 CI 가 진짜로 부른다**(`mcp_server/roundtrip.py`, ci-windows.yml 의 「MCP
  왕복」). curl 로는 멀쩡한데 도구로는 죽는 고장(반환 모양 검증 · 경로 오타)은 불러 봐야만
  드러나고, 정적 검사(`test_mcp_tools.py`)는 그것을 못 잡는다 — 도구가 41개일 때 넷이 죽어
  있던 것이 그 예다. CI 는 마이그레이션 끝난 빈 DB 에 `seed_install.py` 를 심고, 백엔드를
  **reload 없이**(uvicorn 직접 — reload 워커는 부모를 죽여도 소켓을 물고 남는다) 띄우고,
  `scripts/mcp_probe_account.py mint` 가 만든 확인용 토큰으로 읽기 묶음(probe.py)과 쓰기
  사슬(넣고 → 되찾고 → 판정 → 그래프 → 규격 → 물성 제안)을 돈 뒤 `cleanup` 으로 지운다.
  **개발 PC 도 같은 세 명령**이다 — 두 벌이면 한쪽만 고쳐진다. 쓰기 사슬이 만드는 것에는
  「MCP확인」 표가 붙고 cleanup 이 그것으로 찾는다; 토큰은 `TESTSCOPE_PAT` 환경변수로 준다
  (러너에 파일을 안 남기게). 새 쓰기 도구를 더하면 사슬에 한 걸음 더한다.
- **속성 검색이 0건이면 왜 0건인지 말한다**(`attributes/filters.diagnose`,
  `GET /attribute-definitions/diagnose`). 빈 목록은 「아무도 안 적음」 「조건이 좁음」 「단위
  못 바꿈」 「조건끼리 겹쳐 비었음」 을 똑같이 생겼고, 안 가르면 사람도 AI 도 「그런 것
  없습니다」 로 옮긴다 — 가장 흔한 실제는 「아무도 안 적었다」 다. 조건마다 값 적힌 수 ·
  단위 못 바꾼 수 · 그 조건 하나로 걸리는 수와 한 줄을 서버가 세고 서버가 말한다; 화면
  (`AttributeFilterBar empty`)과 MCP(`list_reliability_tests` 가 0건이면 `diagnosis` 를 붙임)가
  같은 엔드포인트를 쓴다. 장비 찾기의 `diagnosis` 와 같은 발상이다.
- **도구를 더하면 그 경로가 범위 표에 있는지 함께 본다**
  (`tests/api/test_agent_surface.py` 의 「새 도구가 쓰는 경로도 범위 안이다」). 표에 없으면
  어느 범위로도 403 이고, 그 사실은 그 도구를 실제로 부른 사람에게만 드러난다. 쓰기가
  열린 것: 기준정보 값·속성 정의·카탈로그(`catalog:write`), 보유 장비·신뢰성 시험
  (`equipment:write`).
- **도구 목록은 매 턴 통째로 실린다.** 63개면 그것만으로 48,000자이고, 그만큼 대화가
  짧아진다 — 그 비용은 도구를 더한 사람 눈에 안 보인다. 그래서 셋을 둔다: ① 「무엇을
  물었나 → 첫 도구」 **길잡이 표를 `instructions` 에** 싣는다(도구 목록과 함께 항상 실리는
  유일한 자리다 — 이름만으로 예순 개를 훑어 고르는 것은 안 된다), ② `get_guide(주제)` 가
  안내를 **대목으로** 준다(통째로 8,000자를 매번 받으면 정작 도구 부를 자리가 준다),
  ③ 바꾸는 도구는 `@writes` 로 달아 `TESTSCOPE_MCP_TOOLS=read` 면 **안 싣는다**(읽기 전용
  토큰에는 어차피 403 이라, 보여 주면 고르는 일만 어려워진다 — 39개 22,000자로 준다).
  긴 이야기는 도구 설명이 아니라 GUIDE.md 의 대목이 한다; 도구 설명에는 **규칙**만 남긴다.
  상한과 표시는 `tests/architecture/test_mcp_tools.py` 가 본다.
- **AI 가 헤매는지는 자취로 잰다**(`mcp_server/eval/`). 답이 아니라 **도구 호출의 자취**를
  채점한다 — 셋 부르는 것과 열둘 부르는 것은 답이 같아도 다른 일이다. 서버가
  `TESTSCOPE_MCP_TRACE` 로 호출마다 한 줄(도구·인자·걸린 시간·오류·**빈손이었나**)을
  남기고(`calltrace.py`, SDK 의 `call_tool` 을 인스턴스에서 덮는다 — 도구마다 데코레이터를
  더 다는 것보다 빠뜨릴 곳이 없다), `eval/questions.json` 의 물음 28개가 기대 자취(시작 도구 ·
  필수 · 금지 · 상한)를 갖고, `eval/score.py` 가 다섯 항목을 센다. **pytest 처럼 「AI 가 보는
  것」 을 고쳤을 때 돌리고**, 기준선(`baseline.json`)과 견준다. CI 에는 안 넣는다: LLM 답이
  매번 조금씩 달라 1~2점은 흔들리고, 빨강/초록으로 자르면 헛경보다. 자취는 기본 꺼짐 —
  운영에서 파일이 끝없이 자라면 안 된다. 도구 이름을 바꾸면 구조 시험이 물음 파일의 낡은
  이름을 잡는다.
- **AI 는 검토함에서 정하지 않는다.** 큐와 줄을 읽는 도구만 두고 확정·의견은 기계 자격으로
  막힌 채 둔다(범위 표에 `/api/review` 가 없다). 고른 것이 곧 카탈로그 정본이 되므로 그
  판단은 사람이 화면에서 한다 — 도구 설명에도 그렇게 적는다.

## 검증

고치고 나서 이 넷을 돌린다.

**큰 파일을 가르거나 옮겼으면 읽기 스모크로 전후를 견준다**(`scripts/smoke_read.py`).
정의가 같고(AST) 시험이 초록이어도 이어 붙인 것이 실제 데이터로 도는지는 못 본다 — 자르기
전 커밋을 `git worktree` 로 꺼내 그쪽에서 `smoke_read.py before.json`, 자른 뒤
`smoke_read.py after.json --diff before.json`. 읽기 엔드포인트 150건이 같아야 한다(다른
것이 요청 id·디스크·버전뿐이면 같다). 2026-09-13 반입 스크립트를 가를 때 이것이
`DEFAULT_ROOT` 가 한 층 어긋난 것을 잡았다.

**마이그레이션을 만들었으면 그 자리에서 개발 DB 에 올린다**(`alembic upgrade head`).
코드 배포와 DB 반영은 다른 일이다 — 서버는 새 코드를 서빙하는데 마이그레이션은
자동이 아니라서, 새 컬럼을 읽는 API 가 500 을 낸다. **명령 사슬에 끼워 두지 말 것** —
앞 단계가 실패하면 조용히 안 돈다.

```powershell
cd backend
.\.venv\Scripts\ruff.exe format . ; .\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\mypy.exe
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m alembic check
cd ..\frontend ; npm run build ; npm test ; npm run lint
```

**`npx tsc --noEmit` 로 대신하지 마라 — 아무것도 안 본다.** `frontend/tsconfig.json` 은
`"files": []` 에 참조만 둔 솔루션 파일이라, 그것을 곧장 부르면 검사할 파일이 0개다. 타입은
`npm run build`(`tsc -b && vite build`)가 본다. 실측(2026-09-23): 그렇게 「통과」 를 보고
푸시했다가 CI 에서 타입 오류 일곱으로 떨어졌다.

`alembic check` 가 여기 있는 이유: **시험은 모델로 표를 만들기 때문에**
마이그레이션이 모델과 어긋난 것을 못 잡는다 — 그 어긋남은 배포하고 나서 500 으로
드러난다.

테스트는 개발 `.env` 의 접속 정보에서 `<이름>_test` 를 파생해 쓴다.
**개발 DB 를 건드리는 테스트는 쓰지 않는다.**

## 문서

- 판단이 갈렸던 결정은 `docs/adr/NNNN-제목.md` 에 남긴다 — 결정·배경·대안·결과.
- **관계의 이유는 `docs/데이터-모델-관계도.html`**, 관계의 정본은 `graph/model.py`, 살아
  있는 그림은 「공통 → 지식 그래프」 다. 문서는 「왜 이렇게 이었나」 를 적고, 무엇이 이어져
  있는지는 화면이 실제 건수와 함께 보여 준다 — 그림을 문서에 또 그리면 표가 하나 늘 때마다
  둘이 어긋난다. 새 관계를 더하면 그 문서의 마지막 절에 이유 한 줄.
- **포트 표는 README 「포트」 절 하나다**(백엔드 8020/8021 · MCP 8022 · 프론트 5200 · PG
  5432/5434 · Ollama 11434). 다른 곳에 또 적지 않는다 — 두 벌이면 한쪽만 고쳐진다.
- 실측으로 드러난 함정은 **근거와 함께** 적는다. "이렇게 하세요" 보다 "이렇게 안
  하면 이런 일이 났다" 가 다음 사람에게 유용하다.
