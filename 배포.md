# 배포

운영 서버에 TestScope 를 올리는 절차. **초기 배포(Day 0)** 와 **업데이트 배포**는
쓰는 스크립트가 다르다.

| 상황 | 스크립트 |
|---|---|
| 서버에 처음 올린다 (DB 도 없다) | `precheck.ps1` -> **`install.ps1`** |
| 이미 돌고 있는 서버를 갱신한다 | `backup.ps1` -> **`deploy.ps1`** |
| 갱신했는데 문제가 생겼다 | **`rollback.ps1`** |
| 백업에서 되돌린다 | **`restore.ps1`** |

릴리스 자산은 **`deploy_package.zip` 하나**다. 코드·SPA·wheel 번들·배포 스크립트가
전부 그 안에 있다. 폐쇄망이면 이 파일만 반입하면 된다.

---

## 0. 사람이 미리 준비할 것

스크립트가 **해 주지 않는 것**은 둘뿐이다.

| | 확인 |
|---|---|
| **PostgreSQL 17** 설치·기동 | `pg_isready` 가 응답하고, 관리자(`postgres`) 비밀번호를 안다 |
| **Python 3.13** 설치 | `py -0p` 목록에 3.13 이 있다 (wheel ABI 가 빌드와 맞아야 한다) |

나머지 — 데이터베이스 생성, 스키마, 온톨로지, 관리자 계정, 방화벽, 실행 스크립트 —
는 `install.ps1` 이 만든다.

### 데이터를 D: 에 두려면 — 설치 전에 정한다

자리가 셋이고, 셋 다 **처음에** 정하는 것이 싸다. 나중에 옮기는 것은 셋 다 서비스를 멈추고
복사하는 일이다.

| 무엇 | 어디서 정하나 |
|---|---|
| **DB 전체**(장비·계정·임베딩 벡터까지 — PostgreSQL 이 갖는 모든 것) | PostgreSQL 설치 프로그램의 **「Data Directory」 를 `D:\PostgreSQL\17\data` 로**. 설치 뒤에는 서비스가 그 폴더를 가리킨다(`postgresql-x64-17`). 프로그램 자체는 C: 에 둬도 된다 |
| 앱 · 가상환경 · **첨부(filestore)** · 로그 · 서비스 정의 | `install.ps1 -AppPath 'D:\Server\TestScope'` — `_venvs` `_data` `_service` `_prev` 가 전부 옆에 생긴다. 스크립트도 `D:\Server	ools\TestScope` 에 |
| Ollama 모델(의미 검색, 1.2GB) | `setup_ollama.ps1 -ModelPath 'D:\Ollama\models'` — 기계 전역 `OLLAMA_MODELS` 로 박힌다. 이미 다른 값이 박혀 있으면 그것을 따른다 |
| 백업 | `backup.ps1 -BackupRoot '<다른 드라이브>'` — **DB 와 같은 드라이브에 두지 않는다.** 그 드라이브가 죽는 날 둘 다 죽는다 |

PostgreSQL 을 이미 C: 에 깔았는데 아직 아무 DB 도 안 만들었다면 지우고 다시 까는 것이
제일 빠르다. DB 가 있으면: 서비스 중지 → 데이터 폴더를 D: 로 복사(권한까지, `robocopy /E /COPYALL`)
→ `pg_ctl.exe unregister -N postgresql-x64-17` 뒤 `register -N postgresql-x64-17 -D D:\…\data`
→ 서비스 시작. 한 DB 만 옮기려면 테이블스페이스(`CREATE TABLESPACE … LOCATION` +
`ALTER DATABASE … SET TABLESPACE`)가 있지만, 이 저장소의 스크립트는 그 길을 자동화하지 않는다.

**PostgreSQL 이 여럿 깔린 서버**(17 과 18 같이)라면 두 가지만 본다. 쓸 판이 어느 **포트**인지
— 두 번째로 깐 것은 5433 을 받는 일이 흔하다(`data\postgresql.conf` 의 `port`) — 그리고
그 포트를 `install.ps1 -DbPort` 로 준다. 설치본 폴더는 스크립트가 **접속한 서버의 판**을 물어
같은 판의 것(서비스 등록 경로에서 찾으므로 D: 에 깔았어도 된다)을 고르니 알려 줄 것이 없다.

디스크는 **3GB 이상** 비어 있어야 한다(앱 2벌 + 가상환경). D: 에 두면 D: 가 그렇다.

PowerShell 은 **관리자 권한**으로 연다. `C:\Server` 에 폴더를 만들고 방화벽 규칙을
추가하기 때문이다.

---

## 1. 초기 배포 (Day 0)

### 1-1. 배포 스크립트 꺼내기

스크립트를 **`C:\Server\tools\TestScope\`** 하위에 둔다. `C:\Server\tools` 바로
아래에 두면 같은 서버에 사는 다른 앱의 `deploy.ps1`·`rollback.ps1` 과 이름이 겹쳐
서로를 덮어쓴다.

```powershell
# 인터넷이 되는 경우
gh release download --repo 4leaf321-nox321/TestScope --pattern deploy_package.zip
Expand-Archive .\deploy_package.zip .\unpacked -Force

New-Item -ItemType Directory -Force -Path 'C:\Server\tools\TestScope'
Copy-Item .\unpacked\install.ps1, .\unpacked\precheck.ps1, `
          .\unpacked\deploy.ps1, .\unpacked\rollback.ps1, `
          .\unpacked\backup.ps1, .\unpacked\restore.ps1, `
          .\unpacked\service.ps1, .\unpacked\setup_ollama.ps1, `
          .\unpacked\install_pgvector.ps1 `
          'C:\Server\tools\TestScope\' -Force
Copy-Item -Recurse .\unpacked\pgvector 'C:\Server\tools\TestScope\pgvector' -Force   # install_pgvector.ps1 이 옆에서 찾는다
```

폐쇄망이면 `deploy_package.zip` 을 USB 등으로 반입해 같은 방식으로 푼다.

### 1-2. 환경 점검

```powershell
cd C:\Server\tools\TestScope
.\precheck.ps1 -AppPath 'C:\Server\TestScope' `
               -DatabaseUrl 'postgresql+psycopg://postgres:<암호>@localhost:5432/testscope'
```

막는 것: Python 3.13 없음 · 디스크 부족 · 폴더 쓰기 권한 없음 · 포트 8020 사용 중 ·
PostgreSQL 도달 불가. **여기서 걸리는 것을 먼저 해결한다** — 설치 도중에 발견하면
반쯤 만들어진 상태가 남는다.

### 1-3. 설치

```powershell
.\install.ps1 -AppPath 'C:\Server\TestScope' -DbPassword '<postgres 암호>'

# 폐쇄망이면 반입한 zip 을 직접 지정
.\install.ps1 -AppPath 'C:\Server\TestScope' -DbPassword '<암호>' `
              -ZipPath 'C:\tmp\deploy_package.zip'
```

바꿀 수 있는 것(기본값): `-DbHost localhost` · `-DbPort 5432` · `-DbName testscope` ·
`-DbUser postgres` · `-Port 8020` · `-AdminEmail admin` ·
`-PythonExe`(3.13 을 자동으로 못 찾을 때) · `-Tag v0.1.0`(특정 버전) ·
`-NoService`(서비스로 등록하지 않고 콘솔로만 띄울 때) · `-NoPgvector`(의미 검색 확장을 안 넣을 때).

install 이 하는 일:

```
1. 코드 배치            zip 을 풀어 <AppPath> 로 (deploy.ps1 을 그대로 쓴다)
2. 폴더 생성            <AppPath>_venvs, <AppPath>_data (filestore·logs)
3. 가상환경             wheel 번들에서 설치 — 인터넷 없이도 된다
                        MCP 서버가 있으면 **별도 venv** 로 하나 더 만든다
4. backend\.env 생성    이미 있으면 건드리지 않는다. JWT_SECRET 은 난수
5. 데이터베이스 생성    CREATE DATABASE ... ENCODING 'UTF8' (이미 있으면 넘어간다)
6. alembic upgrade head 스키마 전체
7. 설치 시드            온톨로지 축 · 조건 정의 · 사양 그룹·정의 · 뿌리 부서 · 관리자 계정
8. 방화벽               TCP 8020 인바운드 허용
8-b. pgvector           DB 가 이 PC 에 있으면 의미 검색용 확장을 넣는다(실패해도 설치는 계속 — 의미 검색만 꺼진다)
9. 서비스 등록          Windows 서비스 TestScope · TestScope-Worker(·TestScope-MCP) — 부팅 때 뜨고 죽으면 되살아난다
                        (-NoService 를 주면 건너뛴다. 콘솔용 run_server.ps1·run_worker.ps1 은 늘 함께 들어 있다)
```

**각 단계는 멱등하다.** 중간에 끊겨 다시 실행해도 같은 결과가 나오고, 특히 이미
있는 관리자 계정의 비밀번호를 되돌리지 않는다.

마지막에 **관리자 비밀번호가 한 번만 출력된다.** 다시 표시되지 않으니 그 자리에서
받아 적는다. 아이디는 `admin` 이고(`-AdminEmail` 로 바꾼다), 첫 로그인 시 비밀번호
변경이 강제된다.

받아 적기 전에 창을 닫았다면 다시 설치하지 말고 복구 도구를 쓴다 —
`seed_install.py` 는 **이미 있는 계정의 비밀번호를 되돌리지 않는다.**

```powershell
cd <AppPath>\backend
& '<AppPath>_venvs\backend\Scripts\python.exe' scripts\set_admin.py `
    --email admin --password '<새 비밀번호>'
```

### 1-4. 기동 — 서비스가 한다

`install.ps1` 이 끝나면 앱은 **이미 Windows 서비스로 떠 있다.** 부팅하면 로그인 없이
올라오고, 프로세스가 죽으면 서비스 관리자가 되살린다(10초 → 30초 → 1분 간격, 한 시간
잘 돌면 횟수 초기화). 사람이 켤 것이 없다.

```powershell
cd C:\Server\tools\TestScope
.\service.ps1 -AppPath 'C:\Server\TestScope' -Action status     # 상태
.\service.ps1 -AppPath 'C:\Server\TestScope' -Action restart    # 재시작
.\service.ps1 -AppPath 'C:\Server\TestScope' -Action stop       # 멈춤 (콘솔로 띄워 볼 때)
.\service.ps1 -AppPath 'C:\Server\TestScope'                    # 등록·갱신하고 시작
.\service.ps1 -AppPath 'C:\Server\TestScope' -Action uninstall  # 서비스 제거
```

| 서비스 | 하는 일 | 뜨는 순서 |
|---|---|---|
| `TestScope` | API 와 화면 (포트 8020) — 한 프로세스가 둘 다 서빙한다 | 먼저 |
| `TestScope-Worker` | 작업 큐 워커 — 의미 검색 색인처럼 요청 밖에서 시간이 걸리는 일 | 백엔드와 무관(DB 만 있으면 됨) |
| `TestScope-MCP` | AI 연결 (포트 8022). 패키지에 MCP 가 있을 때만 | `TestScope` 뒤에 |

워커가 없어도 앱은 돈다 — 작업이 큐에 쌓이기만 하고 처리되지 않는다. 「서버」 화면의
「작업 큐」 에 대기가 쌓이고 실행 중이 0 이면 워커가 안 떠 있는 것이다.

서비스 정의는 `<AppPath>_service\` 에 있다 — **앱 폴더 밖**이라 배포가 앱 폴더를 통째로
바꿔도 서비스는 그대로다. 래퍼(WinSW)의 stdout/stderr 는 `<AppPath>_data\logs\service\`,
앱 로그는 전처럼 `<AppPath>_data\logs\` 다. 서비스가 자꾸 죽으면 앞엣것을 먼저 본다 —
파이썬이 뜨기도 전에 죽는 이유(.env 없음, 포트 점유)는 거기에만 찍힌다.

`services.msc` 에서도 보인다. **거기서 멈추고 켜도 되지만, 창을 열어 둔 채로
`service.ps1` 을 돌리지 말 것** — 서비스를 다시 만드는 동안 그 창이 옛 정의를 붙잡아
「삭제 표시」 상태로 남는다.

**콘솔로 띄우는 길도 남아 있다.** 문제를 눈으로 볼 때, 또는 `-NoService` 로 설치했을 때:

```powershell
.\service.ps1 -AppPath 'C:\Server\TestScope' -Action stop     # 서비스가 있으면 먼저 멈춘다 — 포트가 하나다
cd C:\Server\TestScope ; .\run_server.ps1     # 창 1 — API 와 화면
cd C:\Server\TestScope ; .\run_worker.ps1     # 창 2 — 작업 큐 워커
cd C:\Server\TestScope ; .\run_mcp.ps1        # 창 3 — AI 연결 (안 쓰면 생략)
```

이 창을 닫으면 서버가 멈추고, 재부팅하면 다시 켜야 한다. 서비스로 돌아가려면
`.\service.ps1 -AppPath 'C:\Server\TestScope' -Action start`.

**MCP 는 없어도 된다.** AI 가 무슨 시험이 되는지 묻고 카탈로그를 채우는 자리다 — 이
서비스가 꺼져 있으면 AI 쪽에서 「연결할 수 없다」 가 뜰 뿐, 앱은 멀쩡히 돈다. 기본
포트는 8022(백엔드 +2)이고 `backend\.env` 의 `MCP_PORT` 로, 듣는 자리는 `MCP_HOST` 로
바꾼다(밖에 열려면 `0.0.0.0` — 바꾼 뒤 `service.ps1` 을 다시 돌려야 서비스 정의에
반영된다). 같은 두 키를 개발용 `mcp_server\run_mcp.ps1` 도 읽으므로, 운영에서 쓸 값을
개발에서 먼저 확인할 수 있다. 8030 이었던 것을 옮겼다 — CrossAXTF 의 운영 포트와 같은
PC 에서 겹쳤다(2026-09-12). 서비스 없이 MCP 만 빼려면 `-NoMcp`.

    등록 주소   http://<서버>:8022/mcp
    자격        화면의 「내 정보 → 토큰」 에서 발급 (범위: read · catalog:write)

**만능 토큰이 없다.** 서버는 받은 토큰을 백엔드로 나르기만 하므로, 부르는 사람의
권한이 곧 한계다. 계정 관리와 서버 설정은 어느 범위로도 안 열린다.

### 1-5. 확인

```powershell
Invoke-WebRequest -UseBasicParsing http://127.0.0.1:8020/api/health
```

그다음 브라우저에서 `http://<서버주소>:8020/` 로 들어가:

1. `admin` 으로 로그인 -> 비밀번호 변경 화면이 뜬다 -> 바꾼다
2. 카탈로그를 들인다(2-3-b) — 시험 항목 94종·계열 265·기종 891·물성 271 이 한 번에
   들어온다. 손으로 계열을 만들 필요가 없다
3. **카탈로그 > 장비 계열**에서 들어온 계열을 하나 열어 시험 항목·기종을 본다
4. **보유 장비**에서 그 기종을 골라 장비를 등록한다 — 계열의 시험 항목이 복사된다
5. **장비 찾기**에서 그 조건을 물어 장비가 나오는지 본다

여기까지 되면 계정·온톨로지·장비·시험 항목·검색이 전부 통한 것이다.

데모 데이터로 먼저 보고 싶으면:

```powershell
cd C:\Server\TestScope\backend
& '..\..\TestScope_venvs\backend\Scripts\python.exe' scripts\seed_demo.py
# 지울 때: scripts\seed_demo.py --purge
```

---

## 2. 업데이트 배포

### 2-1. 앱을 먼저 중지한다 — 서비스면 배포가 알아서 한다

서비스(`TestScope`·`TestScope-MCP`)로 돌고 있으면 **`deploy.ps1` 이 스스로 멈추고 끝에
다시 올린다.** 할 일이 없다. 배포가 staging 검사에서 멈추면(패키지 불량·Python 불일치)
서비스도 그 자리에서 되살린다 — 배포는 안 됐는데 앱까지 내려가 있는 상태를 안 만든다.

콘솔(`run_server.ps1`)로 띄웠으면 그 창을 닫는다. 윈도우는 실행 중인 파일을 잠그므로,
켜 둔 채 배포하면 `deploy.ps1` 이 **거부한다** — 부분 이동으로 망가지는 것을 막으려고
일부러 그렇게 만들었다. 무엇이 폴더를 잡고 있는지 이름을 대 주므로 그것을 보고 닫는다.

### 2-2. 백업

```powershell
cd C:\Server\tools\TestScope
.\backup.ps1 -AppPath 'C:\Server\TestScope' -BackupRoot 'D:\TestScope-backup'
```

**DB 와 파일스토어를 같은 시각에 받는다.** 둘 중 하나만 받으면 복구되지 않는다.

### 2-3. 배포

```powershell
.\deploy.ps1 -AppPath 'C:\Server\TestScope'                    # 최신 릴리스
.\deploy.ps1 -AppPath 'C:\Server\TestScope' -Tag v0.2.0        # 특정 버전
.\deploy.ps1 -AppPath 'C:\Server\TestScope' -ZipPath 'C:\tmp\deploy_package.zip'
```

deploy 가 하는 일:

```
0. 서비스 중지     TestScope-MCP → TestScope 순. 콘솔로 띄운 것은 사람이 닫는다
1. 잠금 확인       실제로 이름을 바꿔 본다. 못 옮기면 무엇이 잡고 있는지 알려준다
2. staging 검사    wheel · frontend\dist · Python 버전. **여기까지 운영 폴더는 그대로**
3. 교체            현재 -> _prev, staging -> 운영 (원자적 이름 변경)
4. .env 이어받기   _prev 에서 복사한다 — 접속 정보는 패키지에 없다
5. 가상환경        requirements 해시가 바뀔 때만 다시 만든다
6. 마이그레이션    alembic upgrade head
7. 온톨로지 씨앗   새 축·조건·사양 정의는 행이라 마이그레이션으로 안 들어간다
7-b. 의미 검색 표  pgvector 가 있으면 search_chunks 를 만들고(없으면 조용히 넘어감), 비어 있으면 첫 색인 작업을 넣는다
8. 서비스 시작     멈췄던 것만, TestScope → TestScope-Worker → TestScope-MCP 순
```

마이그레이션이 실패하면(exit 10) **서비스는 멈춘 채 둔다** — 반쯤 적용된 DB 위에 새 코드를
올리지 않는다. 롤백한 뒤 `service.ps1 -Action start`.

**2 번이 핵심이다.** Python 버전이 안 맞거나 SPA 가 빠진 패키지는 운영 폴더를
건드리기 전에 걸린다 — 그때 서버는 아무것도 바뀌지 않은 채로 남는다.

### 2-3-b. 카탈로그 들이기 (릴리스에 카탈로그 변경이 있을 때)

계열·기종·물성·시험법은 **데이터**라 배포가 자동으로 넣지 않는다 — 언제 들일지는 사람이
정한다. 패키지에 `source\catalog` 가 들어 있으니 서버에서 직접 돌린다. **안 돌리면 화면이
말한다**: 관리자 홈의 「남은 일」 과 「서버」 화면에 「카탈로그 반입이 정본보다 뒤짐」 이 뜬다
(반입이 끝에 정본의 지문을 감사 기록 `catalog.imported` 로 남기고, 서버가 파일의 지문과
견준다). 그 경고가 없으면 이 절은 건너뛰어도 된다.

```powershell
cd C:\Server\TestScope\backend
& '..\..\TestScope_venvs\backend\Scripts\python.exe' scripts\import_catalog.py --dry-run   # 무엇이 들어가나
& '..\..\TestScope_venvs\backend\Scripts\python.exe' scripts\import_catalog.py
& '..\..\TestScope_venvs\backend\Scripts\python.exe' scripts\seed_test_item_axes.py   # 시험 항목의 검색축 첫 값 (더하기만 — 사람이 정한 것은 안 뺌)
```

멱등이고 **있는 값은 안 덮는다** — 운영에서 손으로 고친 계열·기종은 그대로 남고, 새로 생긴
것만 더해진다. 반입이 못 정한 것(어느 시험의 규격인가 · 검색축 · 물성 연결 의심 · 정의로 올릴
사양)은 **「관리 → 검토함」** 에 후보·추천·근거와 함께 서고, 도메인 전문가가 거기서 고른다.
개발에서 이미 고른 것은 정본(`source\catalog\proposals`)에 실려 와서 다시 묻지 않는다. 물성↔시험 항목 연결은 개발에서 확인한 것을 `export_property_links.py` 로
정본에 되돌려 써 두었으면 **확인된 채로** 들어오고, 안 한 것은 「제안」 으로 들어와 「물성 항목」
화면에서 확인한다. 개발 DB 를 통째로 복원해 오는 길은 쓰지 않는다 — 운영의 보유 장비·계정이
날아간다.

### 2-4. 롤백

```powershell
.\rollback.ps1 -AppPath 'C:\Server\TestScope'
```

**파일만 되돌아간다. 마이그레이션은 취소되지 않는다.** 되돌린 버전의
`requirements.txt` 가 다르면 `venv_sync.ps1 -Force` 를 한 번 실행한다. 서비스는 롤백이
멈추고 다시 올린다.

### 2-5. 서비스 없이 깔린 서버에 서비스 더하기 (v0.5.0 이전 설치)

전에는 콘솔(`run_server.ps1`)뿐이었다. 새 패키지로 `deploy.ps1` 을 한 번 돌린 뒤(WinSW 가
`<AppPath>\tools\` 에 들어온다) 관리자 PowerShell 에서:

```powershell
.\service.ps1 -AppPath 'C:\Server\TestScope'
```

콘솔 창이 떠 있으면 먼저 닫는다 — 포트가 하나다.

### 2-6. 의미 검색 켜기 (선택, 한 번만)

**안 해도 된다.** 검색과 이름 찾기(`/resolve`)는 이름·별칭으로 바로 돈다. 부품 둘
(임베딩 엔진 Ollama · PostgreSQL 확장 pgvector)을 깔면 **글자가 안 겹쳐도 뜻이 가까운 것**이
얹힌다 — 「HAST」 로 「고가속 습열」 을, 「thermal shock」 으로 「열충격」 과 열충격 챔버 계열을
찾는 일이 된다. 시험 항목·물성·계열·기종·규격·보유 장비·신뢰성 시험을 카드로 만들어 색인한다
(ReportArchive·MatNexus 와 같은 방식 — 잡 큐 + 워커 + pgvector + Ollama bge-m3).

**배포와 별개이고 한 번만 한다.** 넷이다(관리자 PowerShell, `C:\Server\tools\TestScope`):

```powershell
# ① pgvector — 파일 세 개를 PostgreSQL 에 넣고 확장을 켠다. 패키지에 PG17 용이 들어 있다.
#    install.ps1 이 DB 가 이 PC 에 있으면 이미 했다(8-b). 원격 DB 면 그 서버에서 돌린다.
.\install_pgvector.ps1 -DatabaseUrl 'postgresql://postgres:<암호>@localhost:5432/testscope'

# ② Ollama — 설치 + SYSTEM 으로 부팅 때 뜨는 작업 + bge-m3(1.2GB). 이미 11434 에서 응답하는
#    Ollama 가 있으면(다른 앱이 올린 것) 그것을 쓰고 서비스를 또 등록하지 않는다.
.\setup_ollama.ps1
.\setup_ollama.ps1 -CheckOnly        # 아무것도 안 바꾸고 상태만

# ③ backend\.env 에 setup_ollama.ps1 이 찍어 준 네 줄을 적는다
#    EMBEDDING_BACKEND=ollama
#    OLLAMA_BASE_URL=http://127.0.0.1:11434
#    EMBEDDING_MODEL=bge-m3
#    EMBEDDING_DIM=1024

# ④ 표를 만들고 첫 색인을 넣은 뒤 서비스를 다시 띄운다(워커가 .env 를 다시 읽고 색인을 채운다)
cd C:\Server\TestScope\backend
& '..\..\TestScope_venvs\backend\Scripts\python.exe' scripts\ensure_semantic_schema.py
cd C:\Server\tools\TestScope
.\service.ps1 -AppPath 'C:\Server\TestScope' -Action restart
```

확인은 「관리 → 서버」 의 「의미 검색」 줄이다 — `ollama · 색인 2,309개 (test_item 96 · …)` 처럼
나오면 끝. 카탈로그 2,300 카드를 bge-m3 로 색인하는 데 개발 PC 에서 90초쯤 걸렸다. 그 뒤로는
하루 한 번, 그리고 카탈로그 반입이 끝날 때 워커가 다시 색인한다.

**부품이 없어도 배포는 실패하지 않는다.** `deploy.ps1` 이 표를 만들려다 확장이 없으면 조용히
넘어가고, 화면은 「의미 검색: pgvector 없음」 이라고 말한다.

**PostgreSQL 이 17 이 아니면** 패키지의 DLL 이 안 맞는다(메이저 판에 묶인다). 개발 PC 에서
`build_pgvector.ps1` 로 그 판을 빌드해 `scripts\deploy\pgvector\pg<판>` 에 넣고 커밋하면 다음
패키지부터 담긴다 — VS 2019 Build Tools 와 옛 Windows SDK(10.0.19041) 함정은 스크립트 머리에.

**폐쇄망**: pgvector 는 zip 안에 있다. Ollama 설치본(OllamaSetup.exe)과 bge-m3 모델은 인터넷이
되는 PC 에서 `setup_ollama.ps1` 을 한 번 돌린 뒤 `C:\ProgramData\Ollama\models`(또는 그 PC 의
`OLLAMA_MODELS`) 폴더를 통째로 반입하고, 서버에서 `setup_ollama.ps1 -ModelPath <그 폴더>` 로 잇는다.

---

## 3. 백업과 복구

### 3-1. 매일 받게 하기

작업 스케줄러에 등록한다. **앱 프로세스에 넣지 않는다** — 앱이 죽은 날 백업도
조용히 죽는다.

```powershell
$action  = New-ScheduledTaskAction -Execute 'powershell.exe' `
  -Argument '-NoProfile -ExecutionPolicy Bypass -File C:\Server\tools\TestScope\backup.ps1 -AppPath C:\Server\TestScope -BackupRoot D:\TestScope-backup'
$trigger = New-ScheduledTaskTrigger -Daily -At 03:00
Register-ScheduledTask -TaskName 'TestScope Backup' -Action $action -Trigger $trigger `
  -RunLevel Highest -User 'SYSTEM'
```

### 3-2. 복구를 한 번 해 본다

**받아만 두고 복구를 해 본 적이 없는 백업은 백업이 아니다.** 확인용 DB 로 풀어
본다 — 운영은 건드리지 않는다.

```powershell
.\restore.ps1 -BackupRoot 'D:\TestScope-backup' -DbName testscope_restore_check
```

실제 복구(운영을 덮어쓴다):

```powershell
.\restore.ps1 -BackupRoot 'D:\TestScope-backup' -DbName testscope `
              -AppPath 'C:\Server\TestScope' -Force
```

---

## 4. 겪게 될 것들

**`.env` 에 BOM 이 붙으면 첫 줄 키가 조용히 무시된다.** PowerShell 5.1 의
`Set-Content -Encoding utf8` 과 메모장이 BOM 을 붙인다. `APP_ENV` 가 기본값으로
떨어져 운영이 development 로 뜨고, reload 가 켜진 채 돈다. `install.ps1` 은 BOM 없이
쓰지만 사람이 나중에 고칠 때 그렇게 된다.

**`--AppPath` 로 쓰면 조용히 다른 값이 들어간다.** PowerShell 매개변수는 대시가
하나다. 두 개로 쓰면 그 글자 자체가 첫 위치 매개변수에 들어가고 진짜 경로는 다음
매개변수(`-Repo`)로 밀린다 — 스크립트가 그 자리에서 멈추게 만들어 뒀다.

**DB 가 코드보다 뒤처지면 화면이 500 을 낸다.** 기동 로그와 서버 화면이 그것을
말해 주지만, 그 전에 다른 화면을 먼저 열면 원인이 안 적힌 500 을 본다.
`deploy.ps1` 이 매번 `alembic upgrade head` 를 돌리므로 정상 배포에서는 안 생긴다 —
`-SkipMigrations` 를 준 뒤에 잊는 경우가 문제다.

**새 버전 배치에서 「`_staging` 경로에 대한 액세스가 거부」 가 뜨면 몇 초 뒤 다시.**
압축을 풀자마자 백신·인덱서가 새 파일을 훑는 동안은 폴더 이름 바꾸기가 거부된다.
v0.7.1 부터 배포가 스스로 다섯 번(2·4·6·8초 늘려 가며) 다시 시도하고, 끝내 안 되면 옮겨 둔 현재
설치를 제자리로 돌리고 서비스를 다시 올린다. 그 전 판에서 여기서 멈췄으면 상태가
「운영 폴더는 없고 `_prev` 만 있음」 이 되는데, 이때 그대로 다시 돌리면 첫 배포로 읽어
`.env` 를 이어받지 않고 마이그레이션이 `DATABASE_URL` 없이 실패한다. 손으로 고치는 길:

```powershell
Copy-Item D:\Server\TestScope_prev\backend\.env D:\Server\TestScope\backend\.env
.\deploy.ps1 -AppPath 'D:\Server\TestScope'      # 이제 기존 설치가 있으니 정상 경로로 돈다
```

v0.7.1 부터는 그 상태를 알아보고(`_prev` 만 있음 → 「중단된 배포를 이어서 합니다」) `.env`
를 `_prev` 에서 가져온다.

**wheel 은 Python 마이너 버전에 묶인다.** 서버에 3.13 이 없으면 배포가 staging
단계에서 거절하고 운영 폴더는 그대로 남는다. 서버 파이썬을 바꿀 수 없으면 CI 의
`.python-version` 을 그 버전으로 맞춰 새 릴리스를 만든다.

**gh 인증은 서버마다 따로다.** 운영 서버는 개발 PC 와 다른 계정으로 도는 일이
흔하고, 비공개 저장소면 `gh auth login` 없이는 받을 수 없다. 그럴 때는 다른 PC 에서
zip 을 받아 옮기고 `-ZipPath` 를 쓴다 — 그쪽이 원래 의도한 길이다.
