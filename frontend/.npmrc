# openapi-typescript 7.x 는 peer 로 typescript ^5.x 를 요구하는데 이 프로젝트는
# TypeScript 6 을 쓴다. 코드 생성 도구라 앱 런타임에 영향이 없고 생성 결과도
# 정상이라 예외로 둔다. 이 설정이 없으면 CI 의 `npm ci` 가 ERESOLVE 로 멈춘다.
#
# openapi-typescript 가 TS6 을 지원하면 이 파일을 지운다.
legacy-peer-deps=true
