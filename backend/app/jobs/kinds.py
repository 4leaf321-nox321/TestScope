"""작업 종류 이름.

**모듈 사이의 유일한 접점이다.** 반입 스크립트가 검색 모듈의 함수를 직접 부르면 둘이
묶인다. 대신 큐에 이름과 payload 를 넣기만 하고, 그 이름을 처리하는 쪽은 검색
모듈이 스스로 등록한다(`app/modules/search/jobs.py`).

이름을 문자열 리터럴로 흩뿌리지 않고 여기 모으는 이유는, 오타가 나면 작업이
조용히 `failed` 로만 쌓이기 때문이다.
"""

from __future__ import annotations

#: 의미 검색 색인 다시 만들기 — payload 없음
#:
#: 시험 항목·계열·기종·규격·물성·보유 장비·신뢰성 시험을 카드로 만들어 임베딩해
#: `search_chunks` 를 채운다. **저장 요청 안에서 안 하는 이유**: 임베딩 왕복이 수백 ms 라
#: 저장하는 사람이 그 대가를 매번 치르게 된다. 주기로도 돈다 — 카탈로그가 바뀌었는데
#: 색인이 옛 것이면 검색이 조용히 옛 답을 준다.
SEARCH_REINDEX = "search.reindex"
