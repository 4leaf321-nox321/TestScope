"""카탈로그 반입 — 단계별로 나눈 것. 진입은 `scripts/import_catalog.py`.

한 파일이 2,051줄이 되어 한 단계를 고치려면 전부를 읽어야 했다. 단계(온톨로지 → 정의 →
계열 → 기종 → 관계 → 물성)가 이미 함수 경계였으므로 그 경계로 자른다. **글자는 그대로다** —
갈라 나온 날의 dry-run 요약이 자르기 전과 같은 것을 확인했다.
"""
